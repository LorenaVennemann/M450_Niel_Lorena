name: Java IC Niel Test

on:
  push:
    branches:
      - niel_test
  pull_request:
    branches:
      - niel_test

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set working directory 
        run: cd ci-cd/recipe-planner-fronend-and-backend/recipe-planner-backend

      - name: Set up JDK 20
        uses: actions/setup-java@v2
        with:
          java-version: '20'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn clean install

      - name: Run tests
        run: mvn test

      - name: Generate test report
        run: mvn surefire-report:report

      - name: List target directory
        run: ls -R target/reports

      - name: Upload test 
        uses: actions/upload-artifact@v4.5.0
        with:
          name: test-reports
          path: ci-cd/recipe-planner-fronend-and-backend/recipe-planner-backend/target/surefire-reports